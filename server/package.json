{
  "name": "server",
  "version": "1.0.0",
  "description": "Web-API server from a JSON db.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "json-server --watch db.json"
  },
  "author": "",
  "license": "UNLICENSED",
  "dependencies": {
    "json-server": "^0.14.0"
  }
}
